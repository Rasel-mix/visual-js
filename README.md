# visual-js
jqery plugins library

Вызов аккордеона
```javascript
$(function() {
  $('.accordion').accordion(
    accordionsOptions = {
      itemClass: 'accordion-item',
      dataClass: 'accordion-data',
      openClass: 'open',
      speedUp: 500,
      speedDown: 500
    }
  );
});
```
